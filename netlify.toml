[build]
  base    = "docs/"
  publish = "docs/public/"
  command = "gatsby build --prefix-paths && mkdir -p docs/scalajs && mv public/* docs/scalajs && mv docs public/"
[build.environment]
  NPM_VERSION = "6"
